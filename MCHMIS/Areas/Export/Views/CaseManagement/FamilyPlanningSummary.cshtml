@using MCHMIS.Areas.Reports.ViewModels

@model MCHMIS.Areas.Reports.ViewModels.CaseManagementListViewModel
@{
    var summaries = Model.Summaries.Where(i => i.Id == Model.FPMethodId || Model.FPMethodId == null).ToList();
}

@if (Model.Summaries != null)
{
    <table id="table-only" class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>
                    Category
                </th>
                <th width="200">
                    @Html.DisplayNameFor(model => model.Summary.Beneficiaries)
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in summaries)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Beneficiaries)
                    </td>
                </tr>
            }
            @if (!Model.Summaries.Any())
            {
                <tr>
                    <td class="dataTables_empty" colspan="2">
                        No data found.
                    </td>
                </tr>
            }
        </tbody>
        @if (summaries.Any())
        {
            <tfoot>
                <tr>
                    <td colspan="1">
                        <strong>Total</strong>
                    </td>
                    <td>
                         <strong>@Model.Summaries.Sum(i => i.Beneficiaries).ToString("##,###")</strong>
                    </td>
                </tr>
            </tfoot>
        }
    </table>

}